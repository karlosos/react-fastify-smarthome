{
  "$id": "map-point.json",
  "title": "Map point",
  "type": "object",
  "description": "Coordinates for a sensor placed on map",
  "required": ["x", "y"],
  "properties": {
    "x": {
      "type": "number",
      "description": "The width value in percentages according to the image",
      "minimum": 0,
      "maximum": 100,
      "example": 56
    },
    "y": {
      "type": "number",
      "description": "The height value in percentages according to the image",
      "minimum": 0,
      "maximum": 100,
      "example": 14
    }
  }
}

